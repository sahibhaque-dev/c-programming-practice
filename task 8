#include<stdio.h>
int main (){

    int digit , num;
    int real_digit[10]={0};

    printf("enter the number = ");
    scanf("%d",&num);

  while(num>0){
    digit=num%10;
    real_digit[digit]++;
    num=num/10;
  }
            printf("The unique elements found in the array are:");

    for (int i =0;i<10;i++){
          if(real_digit[i]==1){
            printf(" %d",i);
          }
    }

return 0 ;
}
