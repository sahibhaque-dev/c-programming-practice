#include<stdio.h>
int main (){

    int n =5;
    int arr[n];
    for (int i =0;i<n;i++){
       printf("enter %d number = ",i+1);
       scanf("%d",&arr[i]);
    }

    int pos ;
    printf("enter the delete position = ");
scanf("%d",&pos);

       for (int i =pos-1;i<n-1;i++){

       arr[i]=arr[i+1];
    }

     printf("out put= ");
    for (int i =0;i<n-1;i++){

       printf("%d,",arr[i]);
    }

return 0 ;
}
